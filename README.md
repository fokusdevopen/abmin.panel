# Админ Панель - Цифровое Агентство

Полноценная веб-админ панель для управления цифровым агентством.

## 🚀 Деплой на GitHub Pages

### Важно! Перед деплоем:

1. **Измените `package.json`** - замените `YOUR_USERNAME` на ваш GitHub username:
```json
"homepage": "https://YOUR_USERNAME.github.io/admin.panel"
```

2. **Измените `vite.config.ts`** - если ваш репозиторий называется не `admin.panel`, измените base:
```ts
base: '/admin.panel/',  // замените на название вашего репозитория
```

3. **Настройте GitHub Pages:**
   - Перейдите в Settings → Pages
   - Source: Deploy from a branch
   - Branch: `gh-pages` (или используйте GitHub Actions)
   - Folder: `/ (root)`

### Автоматический деплой через GitHub Actions:

1. Закоммитьте и запушьте код:
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

2. GitHub Actions автоматически соберёт и задеплоит проект

### Ручной деплой:

```bash
npm install
npm run build
npm run deploy
```

## 📝 Локальная разработка

```bash
npm install
npm run dev
```

## 🔧 Исправление белой страницы

Если видите белую страницу на GitHub Pages:

1. ✅ Используется `HashRouter` вместо `BrowserRouter` (уже исправлено)
2. ✅ Настроен `base` в `vite.config.ts` (уже исправлено)
3. ✅ Обновите `package.json` с вашим username
4. ✅ Пересоберите проект: `npm run build`
5. ✅ Задеплойте: `npm run deploy`

## 📄 Лицензия

MIT

